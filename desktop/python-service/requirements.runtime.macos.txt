# macOS-only runtime extras. Layered on top of requirements.runtime.txt.
# torchcodec provides the default torchaudio backend on macOS; there is no
# official Windows wheel so Windows builds fall back to soundfile instead.
torchcodec==0.11.0
