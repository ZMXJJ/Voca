# macOS-only runtime extras. Layered on top of requirements.runtime.txt.
# Torch was fully removed on macOS: TTS runs on the C++ (llama.cpp-omni) engine,
# ASR on ONNX, and denoise on sherpa-onnx DPDFNet — none need PyTorch. Audio I/O
# uses soundfile/librosa, so torchaudio/torchcodec (and their ffmpeg) are gone too.
onnxruntime==1.24.4
