# Windows-only runtime extras. Layered on top of requirements.runtime.txt.
# TTS runs on the bundled C++ TTS server (Vulkan build); denoise runs on
# sherpa-onnx (shared with macOS, in the base requirements).
#
# ASR pins CPUExecutionProvider on every platform — nothing in the shipped
# runtime requires CUDA — so this is the plain CPU wheel, same as macOS.
# It used to be onnxruntime-gpu, whose CUDA/TensorRT execution providers were
# never exercised once ASR stopped hard-requiring an NVIDIA GPU.
onnxruntime==1.24.4
